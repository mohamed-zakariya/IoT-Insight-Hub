apiVersion: v1
kind: Service
metadata:
  name: backend-service
spec:
  selector:
    app: backend-deployment
  ports:
    - port: 8080        # external port for service
      targetPort: 80    # container port
  type: LoadBalancer
