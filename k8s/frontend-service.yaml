apiVersion: v1
kind: Service
metadata:
  name: frontend-service
spec:
  selector:
    app: frontend-pod
  ports:
    - port: 4200         
      targetPort: 80     
      nodePort: 30081    
  type: NodePort         
